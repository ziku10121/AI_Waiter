export enum ENUM_WS_LOCATIONS {
  photoSocket = 'photoSocket',
  orderSocket = 'orderSocket',
  commandSocket = 'commandSocket',
  // userSocket  = 'userSocket'
}

export enum ENUM_CONNATION_RESULT {
  wsResult = 'wsResult',
  ws       = 'ws'
}
